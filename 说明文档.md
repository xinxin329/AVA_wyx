# Project-Ava 单卡复现使用说明

## 1. 项目说明

本目录是在 AVA（Agentic Video Analytics）开源代码基础上整理的单卡复现版本，主要用于复现 AVA-100 数据集中的两个长视频：Wildlife1 和 Traffic1。当前实验已经完成视频抽帧、片段描述、语义事件合并、实体与关系抽取、事件知识图谱构建、SA 检索问答、CA 视觉复核以及局部诊断分析。

本复现没有重新训练模型，所有步骤均为模型推理、文本相似度计算、向量编码、知识图谱构建和问答评测。

项目目录：

```text
/root/gpufree-data/Project-Ava-main
```

数据目录：

```text
/root/gpufree-data/AVA100
```

缓存和实验结果目录：

```text
/root/gpufree-data/AVA_cache/AVA100
```

本项目中两个视频的编号如下：

```text
Wildlife1：video_id=5，共 8 道官方问题
Traffic1 ：video_id=7，共 12 道官方问题
```

## 2. 运行环境

实验服务器使用单张 NVIDIA RTX 4090 24 GB，Python 环境位于：

```text
/root/Project-Ava-main/venv
```

当前已经验证的主要环境为：

```text
Python       3.10.12
PyTorch      2.10.0+cu128
Transformers 4.57.6
OpenCV       4.10.0
NumPy        1.26.4
```

此外，项目还使用 LMDeploy、BERTScore、Hugging Face Hub、JinaCLIP、NetworkX 等依赖。服务器中的环境已经配好，一般不需要重新安装。

每次进入服务器后先执行：

```bash
cd /root/gpufree-data/Project-Ava-main
source /root/Project-Ava-main/venv/bin/activate

export HF_HOME=/root/gpufree-data/huggingface
export TRANSFORMERS_CACHE=/root/gpufree-data/huggingface
export PYTORCH_ALLOC_CONF=expandable_segments:True
```

建议在 `tmux` 中运行知识图谱构建和完整问答，以免 SSH 断开后任务终止：

```bash
tmux new -s ava_run
```

退出但保留任务：按 `Ctrl+B`，再按 `D`。重新进入：

```bash
tmux attach -t ava_run
```

## 3. 使用的模型及分工

### Qwen2.5-VL-7B-Instruct-AWQ

代码中的名称为 `qwenvl`，本地模型路径配置在 `llms/QwenVL.py`。它负责读取视频帧、生成每个短片段的视觉描述、提取事件和实体信息，并在 CA 阶段重新查看候选事件对应的视频帧。

### Qwen2.5-14B-Instruct-AWQ

代码中的名称为 `qwenlm`，本地模型路径配置在 `llms/QwenLM.py`。它是纯文本模型，主要用于 SA 阶段的智能体式检索、推理和答案生成，不能直接读取视频图像。

### microsoft/deberta-xlarge-mnli

该模型由 BERTScore 调用，用于计算不同 3 秒片段描述之间的语义相似度。当前语义事件合并阈值为 0.65，每个事件最多合并 8 个基础片段。为适配 24 GB 显存，BERTScore 的 batch size 已由默认值降低；这只影响速度，不改变单对文本的相似度定义。

### jinaai/jina-clip-v1

JinaCLIP 负责把事件文本、实体文本和视频帧编码成向量，供后续相似度检索和多模态检索使用。对应封装位于 `embeddings/JinaCLIP.py`。

## 4. 数据与预处理结果

原始视频和官方问题文件位于：

```text
/root/gpufree-data/AVA100/videos/wildlife1.mp4
/root/gpufree-data/AVA100/videos/traffic1.mp4
/root/gpufree-data/AVA100/wildlife.json
/root/gpufree-data/AVA100/traffic.json
```

本次实验统一使用 2 FPS、540×360 的抽帧结果。抽帧后的图像、帧率和视频配置信息分别保存在：

```text
Wildlife1：/root/gpufree-data/AVA_cache/AVA100/5/frames
Traffic1 ：/root/gpufree-data/AVA_cache/AVA100/7/frames

配置文件：/root/gpufree-data/AVA_cache/AVA100/<video_id>/config.json
```

当前 `preprocess_videos.py` 为 Wildlife1 复现做过限制：当数据集为 `ava100` 时只处理 `video_id=5`。因此，现有 Wildlife1 帧可用下面的命令重新生成：

```bash
python preprocess_videos.py --dataset ava100 --num_threads 1
```

Traffic1 的帧已经存在于 `video_id=7` 的缓存目录。若以后需要重新抽取 Traffic1，应先把 `preprocess_videos.py` 中的 `vids = [5]` 改为 `vids = [7]`，或改造成命令行参数后再运行。不要在已有结果仍需保留时直接清空对应缓存目录。

## 5. 完整复现流程

整体处理顺序如下：

```text
原始长视频
  → 2 FPS、540×360 抽帧
  → 每 3 秒组成一个基础片段
  → Qwen-VL 生成片段描述
  → DeBERTa/BERTScore 进行语义事件合并
  → Qwen-VL 生成事件摘要并抽取实体、关系
  → JinaCLIP 编码文本和图像特征
  → 构建事件知识图谱和向量数据库
  → Qwen-LM 执行 SA 检索问答
  → Qwen-VL 执行 CA 视频帧复核
  → 汇总答案、准确率和诊断结果
```

### 5.1 构建事件知识图谱

Wildlife1：

```bash
python graph_construction.py \
  --model qwenvl \
  --dataset ava100 \
  --video_id 5 \
  --gpus 1
```

Traffic1：

```bash
python graph_construction.py \
  --model qwenvl \
  --dataset ava100 \
  --video_id 7 \
  --gpus 1
```

如果相应 `kg` 目录中已经存在完整结果，不要无目的地重跑本步骤，以免覆盖已有实验数据。

### 5.2 运行 SA 问答

SA 必须先于 CA 运行，因为 CA 会读取 SA 产生的候选事件和评分文件。

运行单道问题：

```bash
python query_SA.py \
  --model qwenlm \
  --dataset ava100 \
  --video_id 5 \
  --question_id 0 \
  --gpus 1
```

Wildlife1 全部 8 道题：

```bash
for qid in $(seq 0 7); do
  python query_SA.py --model qwenlm --dataset ava100 --video_id 5 --question_id "$qid" --gpus 1
done
```

Traffic1 全部 12 道题：

```bash
for qid in $(seq 0 11); do
  python query_SA.py --model qwenlm --dataset ava100 --video_id 7 --question_id "$qid" --gpus 1
done
```

### 5.3 运行 CA 问答

确认对应问题的 `sorted_SA_score_result.json` 已经生成后，再运行 CA。

运行单道问题：

```bash
python query_CA.py \
  --model qwenvl \
  --dataset ava100 \
  --video_id 5 \
  --question_id 0 \
  --gpus 1
```

Wildlife1 全部 8 道题：

```bash
for qid in $(seq 0 7); do
  python query_CA.py --model qwenvl --dataset ava100 --video_id 5 --question_id "$qid" --gpus 1
done
```

Traffic1 全部 12 道题：

```bash
for qid in $(seq 0 11); do
  python query_CA.py --model qwenvl --dataset ava100 --video_id 7 --question_id "$qid" --gpus 1
done
```

本复现为了避免视觉上下文过长，在 CA 阶段最多均匀采样 128 帧。该修改用于适配 RTX 4090 的显存和 LMDeploy 的上下文限制。

## 6. 主要代码文件

```text
graph_construction.py          构建事件知识图谱的入口
query_SA.py                    SA 检索、推理和答案生成入口
query_CA.py                    CA 视频帧复核入口
preprocess_videos.py           视频预处理入口

AVA/ava.py                     AVA 主流程和 SA/CA 调用逻辑
AVA/events.py                  片段描述、BERTScore 合并和事件摘要
AVA/entities.py                实体与关系抽取
AVA/tree_search.py             智能体式图检索
AVA/storage.py                 图谱和向量数据读写
AVA/prompt.py                  事件、检索和问答提示词

llms/QwenVL.py                 视觉语言模型封装
llms/QwenLM.py                 文本模型封装
llms/init_model.py             模型名称注册
embeddings/JinaCLIP.py         图文向量模型封装
dataset/ava100.py              AVA-100 数据路径和缓存路径配置

debug_fixed_event_summaries.py Wildlife1 摘要边界局部 A/B 测试
debug_semantic_chunking_analysis.py
                               Wildlife1 语义分块、背景词消融和阈值分析
oracle_time_test.py            Traffic1 正确时间窗口诊断
generate_reproduction_figures.py
                               Wildlife1 统计图和局部知识图谱
generate_traffic1_figures.py   Traffic1 统计图和局部知识图谱
```

## 7. 结果文件保存位置

### 7.1 知识图谱结果

Wildlife1：

```text
/root/gpufree-data/AVA_cache/AVA100/5/kg
```

Traffic1：

```text
/root/gpufree-data/AVA_cache/AVA100/7/kg
```

其中主要文件为：

```text
kg/events/descriptions.json            原始 3 秒片段描述
kg/events/scores.json                  BERTScore 相似度结果
kg/events/events.json                  合并后的事件及事件摘要
kg/entities/entities.json              实体结果
kg/entities/relations.json             关系结果
kg/graph_event_knowledge_graph.graphml  完整事件知识图谱
kg/vdb_events.json                     事件向量数据库
kg/vdb_entities.json                   实体向量数据库
kg/vdb_relations.json                  关系向量数据库
kg/vdb_features.json                   视频帧视觉特征
kg/kg.log                              图谱构建日志
```

### 7.2 SA 与 CA 问答结果

每道题的结果位于：

```text
/root/gpufree-data/AVA_cache/AVA100/<video_id>/questions/<question_id>/
```

主要文件包括：

```text
tree_information.json             智能体检索树和检索路径
SA_score_result.json              SA 候选评分
SA_self_consistency_result.json   SA 多次生成结果
sorted_SA_score_result.json       排序后的 SA 最终结果
CA_score_result.json              CA 候选评分
CA_self_consistency_result.json   CA 多次视觉复核结果
sorted_CA_score_result.json       排序后的 CA 最终结果
```

查看某一道题的最终 SA 或 CA 结果，例如 Traffic1 第 0 题：

```bash
python -m json.tool /root/gpufree-data/AVA_cache/AVA100/7/questions/0/sorted_SA_score_result.json | less
python -m json.tool /root/gpufree-data/AVA_cache/AVA100/7/questions/0/sorted_CA_score_result.json | less
```

### 7.3 诊断实验结果

Wildlife1 的摘要边界和语义分块诊断位于：

```text
/root/gpufree-data/AVA_cache/AVA100/5/kg/events/debug_fixed_event_summaries.json
/root/gpufree-data/AVA_cache/AVA100/5/kg/events/debug_semantic_chunking_analysis.json
/root/gpufree-data/AVA_cache/AVA100/5/kg/events/debug_semantic_chunking_pairs.csv
```

Traffic1 的 Oracle Time 诊断位于：

```text
/root/gpufree-data/AVA_cache/AVA100/7/oracle_time_debug.json
/root/gpufree-data/AVA_cache/AVA100/7/oracle_time_test.log
```

### 7.4 图表与知识图谱可视化

Wildlife1 的统计图和局部知识图谱保存在：

```text
/root/gpufree-data/Project-Ava-main/outputs/figures
```

该目录主要包括：

```text
accuracy_comparison.png            论文结果与复现准确率对比
per_question_correctness.png       各题 SA、CA 正误情况
ca_vote_ablation.png               4 次与 8 次 CA 投票消融
knowledge_graph_local.png/.svg
                                    Wildlife1 事件知识图谱局部图
```

Traffic1 的统计图和局部知识图谱保存在：

```text
/root/gpufree-data/Project-Ava-main/outputs/traffic1_figures
```

该目录主要包括：

```text
accuracy_comparison.png            论文总体结果与 Traffic1 SA/CA 对比
per_question_correctness.png       Traffic1 各题正误情况
answer_distribution.png           预测答案与标准答案分布
ca_vote_distribution.png          各题 CA 候选节点投票分布
oracle_time_diagnostic.png         Oracle Time 诊断结果
knowledge_graph_local.png/.svg
                                    Traffic1 正确时间附近的局部知识图谱
```

Wildlife1 图表还复制了一份到共享目录，便于从实例外部下载：

```text
/root/gpufree-share/Project-Ava-results/figures
```

重新运行可视化脚本时，默认以项目目录下的 `outputs/figures` 和 `outputs/traffic1_figures` 为准；共享目录中的文件属于导出副本，不应作为程序继续计算的输入。

## 8. 当前复现结果概览

Wildlife1 已完成 8 道官方问题的 SA 与 CA 流程。SA 答对 6 道，准确率为 75.0%；在当前本地视觉模型和采样配置下，CA 的结果会随投票次数变化。相关逐题结果以各问题目录中的 `sorted_SA_score_result.json` 和 `sorted_CA_score_result.json` 为准。

Traffic1 已完成 12 道官方问题。SA 答对 4 道，准确率为 33.3%；当前 CA 结果为 0/12，并出现明显的 A 选项偏置。Oracle Time 测试表明，部分错误来自检索时间位置不准，另一些错误在给出正确时间窗口后仍然存在，主要涉及车辆颜色、数量和交通因果关系等细粒度视觉理解。

知识图谱已经成功生成。Wildlife1 图谱包含约 2,254 个事件节点和 3,852 个实体节点；Traffic1 图谱包含约 5,742 个事件节点和 8,179 个实体节点。Wildlife1 的局部 Debug 进一步发现，固定机位的重复背景词会抬高 BERTScore，使前景动物发生变化的片段仍可能被合并；该问题不是完整图谱构建失败，而是语义分块在固定背景场景中的适用边界。

## 9. 运行注意事项

1. `graph_construction.py` 使用 `qwenvl`，不要改成 `qwenlm`，因为知识图谱构建需要读取图像。
2. SA 使用 `qwenlm`，CA 使用 `qwenvl`。
3. CA 依赖 SA 结果；如果缺少 `sorted_SA_score_result.json`，CA 会直接报错。
4. `video_id=5` 对应 Wildlife1，`video_id=7` 对应 Traffic1，不要混用问题文件和缓存目录。
5. 不要随意删除 `kg` 和 `questions` 目录。重新运行同一题之前，应先明确是否需要保留旧结果，并把旧文件移动到单独备份目录。
6. 如果出现 CUDA OOM，先用 `nvidia-smi` 检查是否有其他进程占用显存；不要首先修改模型或删除已有结果。
7. 如果 Hugging Face 再次尝试下载已经存在的模型，检查 `HF_HOME` 是否指向 `/root/gpufree-data/huggingface`，以及代码中的本地模型路径是否正确。
8. 首次运行或长时间运行建议使用 `tmux`，并用另一个终端通过 `watch -n 5 nvidia-smi` 观察显存和 GPU 利用率。
